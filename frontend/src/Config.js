 export const CONFIG = {
  NETWORK_NAME: "Base", // display only
  OPENSEA_API: "https://api.opensea.io/api/v1/assets", // can require API key for heavy use
  ZORA_API: "https://api.zora.co/graphql", // Zora GraphQL endpoint (example)
  // Example default owner or collection to list NFTs from:
  DEFAULT_OWNER: "", // set an address to show owner NFTs
  DEFAULT_COLLECTION: "", // set a collection slug if needed
  FDH_TOKEN: "FDH",
  FDH_POINTS_DAILY: 10, // points per daily check
  SITE_HOST: "https://your-site.example", // used to build referral links (update)
};
